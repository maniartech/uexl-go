package parser_test

import (
	"testing"
)

// TestParser_Old tests that expressions are correctly parsed and evaluated
func TestParser_Old(t *testing.T) {
	// Skip this test for now as we've split parsing from evaluation
	t.Skip("This test needs to be updated to work with the new parser/evaluator separation")

	// This is just a placeholder for the original tests that were expecting
	// the parser to also evaluate expressions
}
